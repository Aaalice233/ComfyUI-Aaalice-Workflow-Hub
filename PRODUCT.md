# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 工作流订阅者：在本地 ComfyUI 中发现、比较、下载并加载公开工作流版本，同时在下载前了解内核、插件和随包资源的兼容状态。
- 工作流作者与发布者：从当前 ComfyUI 画布发布工作流，并维护工作流资料、版本、更新日志、依赖和归档状态。
- 两类用户同等重要，使用同一个插件，但订阅界面与发布者管理界面保持清晰的角色边界。

## Product Purpose

ComfyUI-Aaalice-Workflow-Hub 是 ComfyUI 内的双向工作流中心。它让作者通过可校验的公开协议发布和维护工作流产品，让订阅者在同一入口中订阅公共仓库、浏览历史版本、确认依赖风险并下载工作流。

成功意味着工作流、版本、说明、更新日志、依赖声明和分发产物保持一致且可验证；用户在环境发生变化前拥有知情权和确认权；发布、下载与管理失败具有可定位、可恢复的状态。

## Positioning

产品以“工作流产品”而非仓库或单个文件为核心对象，同时覆盖订阅发现、不可变版本分发、作者发布管理和下载前依赖校验。公开 GitHub 仓库承担可浏览、可备份的正式存档，GitHub Release 承担不可变分发；目录索引只是便于发现的机器可读投影，不是唯一事实来源。

## Operating Context

- 作为 ComfyUI 本地扩展运行，从顶栏“工作流中心”进入内置面板，也可打开独立窗口。
- 订阅者添加含 `workflow-catalog.json` 的公共 GitHub 仓库，搜索、筛选并浏览工作流产品和历史版本。
- 下载前比较当前 ComfyUI 内核与插件环境；插件同步必须先展示计划并由用户确认。
- 下载后的工作流写入当前 ComfyUI 用户的原生 `workflows/` 目录，随包图片写入共享 `input/` 目录，并保留工作流内部引用。
- 作者通过 GitHub Device Flow 登录，从当前画布确认资源、填写资料、复核并发布；拥有仓库写权限的作者可继续管理已发布内容。
- 状态、缓存、下载记录和操作历史按 ComfyUI 用户隔离；插件环境变更因共享 `custom_nodes` 而全局串行。

## Capabilities and Constraints

- 支持公共 GitHub 仓库订阅、聚合目录、搜索筛选、历史版本、更新日志、原生更新通知和按版本下载。
- 支持从当前画布发布，创建 GitHub Release，并原子更新仓库存档、产品资料和目录索引。
- 支持编辑资料、归档、更新日志、Git 依赖 commit 和删除已发布版本；发布版本不可覆盖。
- Git 插件依赖锁定完整 commit，历史 Registry 依赖通过兼容的 ComfyUI-Manager 路径处理；ComfyUI 内核只检测和提示，不自动修改。
- 所有安装、升级、降级和删除操作必须由用户明确确认，并展示真实阶段、日志、错误与恢复信息。
- 仅支持公共 `github.com/{owner}/{repo}` 仓库和受信任的 GitHub HTTPS 主机；不支持私有仓库、其他 Git 服务或任意下载主机。
- 不自动下载模型或 LoRA，不执行工作流仓库、清单或压缩包中的脚本，不收集遥测。
- 下载包必须通过主机、大小、SHA-256、ZIP 路径、符号链接、条目大小、解压总量和压缩比校验；不同内容不得覆盖同名本地工作流或输入文件。
- 移除订阅不会删除已下载文件；本地版本只能由用户明确删除。
- 领域术语以 `docs/context.md` 为准，不使用“我的工作流”混指订阅、产品或本地版本。

## Brand Commitments

- 产品名称保持为 `ComfyUI-Aaalice-Workflow-Hub`，用户界面使用“工作流中心”。
- 面向用户的表达简洁、事实化、可操作，不伪造进度或隐藏原始失败原因。
- 简体中文、繁体中文（台湾）和英文界面跟随 ComfyUI 语言设置；所有用户可见文案由对应运行面的词典统一维护。
- 现有品牌资产为 `assets/icon.png` 和 `assets/banner.png`。

## Evidence on Hand

- 已实现产品行为与范围：`README.md`、`docs/features.md`、`docs/vision.md`。
- 领域语言与协议：`docs/context.md`、`docs/protocol.md`、`schemas/`、`examples/`。
- 安全边界：`docs/security.md`。
- Vue 前端源码与测试：`frontend/`；ComfyUI 宿主桥接层：`web/comfyui/`；Python 后端与测试：`workflow_hub/`、`tests/`。
- GitHub 授权、分发和缓存决策：`docs/adr/`。
- 当前没有可作为产品事实引用的客户名单、用户评价、采用规模、商业指标或性能基准；未来界面和文档不得虚构这些证据。

## Product Principles

1. 工作流产品是核心对象，仓库只是发布载体。
2. 订阅不等于下载，下载不等于修改节点环境。
3. 自动化负责检查和生成计划，用户保留环境变更的最终确认权。
4. 真实状态优先于虚假确定性：阶段、日志、错误和恢复状态必须可见。
5. 不可变发布与无损本地环境同等重要；既不覆盖已发布版本，也不丢失用户本地改动。

## Accessibility & Inclusion

- 当前界面支持键盘操作、可见焦点、语义化导航、对话框语义、可访问名称和加载状态提示。
- 当前未承诺特定 WCAG 等级、浅色主题或完整屏幕阅读器认证；后续不得把这些能力描述为已验证。
