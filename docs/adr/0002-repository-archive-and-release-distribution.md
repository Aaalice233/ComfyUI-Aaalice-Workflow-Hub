# 仓库存档与 Release 分发分离

工作流的所有版本必须以“类别 / 名称 / 版本”目录保存在默认分支，供人直接浏览、备份和下载；根清单只是由这些记录生成的索引。GitHub Release 保留为同一版本生成的 ZIP、LoRA 等分发产物，不再作为工作流的唯一副本。这样增加了多文件原子提交和失败恢复的复杂度，但避免仓库访问者只能逐个翻找 Release，也保留插件安装所需的单文件下载、校验和大文件承载能力。
